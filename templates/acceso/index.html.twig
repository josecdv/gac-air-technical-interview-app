{% extends 'frontend.html.twig' %}

{% block title %}Acceder - Stockify{% endblock %}

{% block body %}
    <div class="col-lg-7 align-self-center">
        <div class="p-3">
            <h2 class="mb-2">Acceder</h2>
            {% if errors %}
                <div class="alert alert-danger alert-dismissible fade show">
                    <ul>
                        {% for error in errors %}
                            <li>{{ error.message }}</li>
                        {% endfor %}
                    </ul>
                    <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                </div>
            {% endif %}
            {% for mensaje in app.flashes('mensaje') %}
                <div class="alert alert-{{app.flashes('css')[0]}} alert-dismissible fade show" role="alert">
                    {{ mensaje }}
                    <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                </div>
            {% endfor %}
            <form method="POST" action="{{ url('acceso_login') }}">
                <div class="row">
                    <div class="col-lg-12">
                        <div class="floating-label form-group">
                            {{form_widget(form.username, {"required": false, "attr":{"placeholder": "", "class": "floating-input form-control", "type": "text"}})}}
                            <label>Usuario</label>
                        </div>
                    </div>
                    <div class="col-lg-12">
                        <div class="floating-label form-group">
                            {{form_widget(form.password, {"required": false, "attr":{"placeholder": "", "class": "floating-input form-control", "type": "password"}})}}
                            <label>Contraseña</label>
                        </div>
                    </div>   
                </div>
                <input type="hidden" name="token" value="{{csrf_token('generico')}}" />
                <button type="submit" class="btn btn-primary">Acceder</button>
                <p class="mt-3">
                    Crear una cuenta <a href="{{ path('acceso_registro') }}" class="text-primary">Registrarse</a>
                </p>
            </form>
        </div>
    </div>
    <div class="col-lg-5 content-right">
        <img src="../template/assets/images/login/01.png" class="img-fluid image-right" alt="">
    </div>
{% endblock %}