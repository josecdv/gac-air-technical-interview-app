{% extends 'dash.html.twig' %}
 {% if add == false %}
     {% set mode = "Eliminar" %}
 {% else %}
     {% set mode = "Añadir" %}
 {% endif %}
{% block title %}{{ mode }} Stock{% endblock %}
{% block content %}

    <h1>{{ mode }} Stock</h1>
    <span>Stock Actual: {{ product.stock }}</span>
    {% if error is defined %}
        <p>{{ error }}</p>
    {% endif %}
    {{ include('products/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_products_index') }}">back to list</a>

    {{ include('products/_delete_form.html.twig') }}
{% endblock %}
